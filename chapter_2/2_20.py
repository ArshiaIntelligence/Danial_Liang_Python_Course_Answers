balance = float(input('Enter the balance: '))
interest_rate = float(input('Enter the interest rate:(in percent) '))
interest = balance * (interest_rate / 1200)

print('The interest for the next month is:', interest)
